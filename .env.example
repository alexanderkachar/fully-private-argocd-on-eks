# Copy to .env and fill in. .env is gitignored.
# Used by scripts/assume-role.sh to assume the admin role for Terraform + Ansible runs.

ROLE_ARN="arn:aws:iam::000000000000:role/role-admin-access"
SESSION_NAME="AWSCLI-Session"
AWS_REGION="us-east-1"
