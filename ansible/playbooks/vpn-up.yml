---
- name: Refresh inventory
  hosts: localhost
  gather_facts: false
  tags: ["always"]
  roles:
    - role: inventory_sync

- name: Associate Client VPN endpoint with subnet
  hosts: localhost
  gather_facts: false
  roles:
    - role: vpn_toggle
      vars:
        vpn_state: associated
